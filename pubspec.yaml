name: color_interpolate
description: A new Flutter package with color trasition background 
version: 0.0.6
homepage: https://github.com/AdrianoBinhara/color_transition

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
